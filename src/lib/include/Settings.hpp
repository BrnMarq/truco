#pragma once

#include <cstddef>

struct Settings
{
    static constexpr size_t hand_size = 3;
    static constexpr size_t win_points = 12;
    static constexpr size_t flower_points = 2;
    static constexpr size_t envido_points = 1;
};